# Firecrawl secrets — required.
# Copy to apps/firecrawl/.env and fill in real values.

# Gates the /admin/<key>/queues UI. Generate with:
#   openssl rand -hex 16
FIRECRAWL_BULL_AUTH_KEY=

# Optional overrides
# FIRECRAWL_PORT=3022
